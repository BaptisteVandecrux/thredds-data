# thredds-data
 
Usage:

```
git clone https://github.com/BaptisteVandecrux/thredds-data.git
cd thredds-data
python thredds_download # for daily site csv files
# or 
python thredds_download --time_resolution hour --data_type site --file_format nc
```